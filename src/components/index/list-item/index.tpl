<div class="cart-list">
    <a href="./detail.html?id={{id}}" class="link">
        <div class="img-box">
            <img src="{{img_url}}" alt="{{good_name}}" />
        </div>
        <div class="title">{{goods_name}}</div>
        <div class="price">￥{{price}}.00</div>
    </a>
</div>